name: Build and Deploy OCI Image

on:
  push:
    tags:
      - 'v*'

jobs:
  registry_publish_workflow_ghcr:
    uses: turbot/steampipe-workflows/.github/workflows/registry-publish-ghcr.yml@also-publish-prereleases
    secrets: inherit
    with:
      releaseTimeout: 60m
